# AI企业营销战略执行智能体 — 开发思路文档（v3.0 合并版）

> 版本：**v3.0（微信小程序开发文档 合并版）** | 日期：2026-08-01
>
> v3.0 合并说明：基于 v2.0 后端链路文档 + 微信小程序开发文档（实际落地内容）+ 149 测试通过后的代码事实
>
> **唯一核心链路：输入企业信息 → 诊断报告 → 翻译成7天执行清单 → 老板照着做 → 周末上传截图复盘**
> 本文档只描述这条链路的实现方案。任何不服务于这条链路的功能/模块/文件，都不在本文档范围。
>
> **正式产品名：营销军师（前端小程序 TabBar 品牌）**
>
> **双模式运行：LLM 模式（需 DEEPSEEK_API_KEY）+ 本地规则引擎模式（Fallback，无需 API Key）**

---

## 一、产品是什么

一个极简的 AI 工具，帮没有营销团队的小企业老板完成一件事：

**输入企业信息 → 得到诊断报告 → 得到7天执行清单 → 照着做 → 周末上传截图复盘 → 得到下周日历**

一句话：**把"想"翻译成"做"，把"做"翻译成"回头看得见"。**

目标用户：小型实体店、家装公司、教培、餐饮、美容、民宿、房产中介、工厂销售。共同特征：懂业务不懂营销，没预算养人。

和市面上"AI写文案"工具的根本区别：它们帮你**做内容**，我们帮你**做决策和排期**。

---

## 二、核心链路（唯一架构）

```
                              ┌── 上传截图/CSV ──┐
                              │                  │
                              ▼                  │
企业输入信息 ──→ AI诊断报告 ──→ 7天执行清单 ──→ 老板执行 ──→ AI复盘
                              │                              │
                              └────── 优化反馈，进入下周 ────┘
```

**只有三个模块，没有第四个。前端小程序明确 3 个 TabBar 入口：**

| 序号 | 模块 | 前端入口 | 干什么 | 输入 | 输出 |
|------|------|----------|--------|------|------|
| 1 | 诊断 | 工作台（home → profile → diagnosis） | 分析企业营销问题，给出方向和策略 | 企业信息表单 | 诊断报告 |
| 2 | 执行引擎 | 执行计划（Tab 2，plan + plan/day） | 把诊断结论翻译成7天任务清单（**核心壁垒**） | 诊断报告 | 7天任务清单 |
| 3 | 复盘 | 复盘（Tab 3，upload + review） | 上传本周数据截图，AI分析并给出下周建议 | 截图/CSV | 复盘报告 |

**明确不做的事**：
- 不做内容生成工坊（红海，不是壁垒）
- 不做AI销售助手（先跑通基础链路再说）
- 不做长期战略规划（诊断报告里带上策略建议就够了）
- 不做多Agent架构（一条Prompt链 + 3 Agents 编排搞定）
- 不做平台API数据接入（用截图/CSV上传代替）
- 不做用户系统/支付/仪表盘

---

## 三、行业方案（地产先行 + 5行业覆盖）

### 3.1 优先级矩阵

| 行业 | 方案深度 | 维度数 | 技能存储模式 | 客户数量 |
|---|---|---|---|---|
| **地产（核心）** | ⭐⭐⭐⭐⭐ 深度 | **6维度** | 目录模式：`skills/real_estate/*.md` | 12+ 别名（中介/房产/置业顾问…） |
| 家装 | ⭐⭐⭐ 基础 | 5维度 | 单文件模式：`industry_skills/renovation.md` | 5 别名 |
| 餐饮 | ⭐⭐⭐ 基础 | 5维度 | 单文件模式：`industry_skills/restaurant.md` | 4 别名 |
| 教培 | ⭐⭐⭐ 基础 | 5维度 | 单文件模式：`industry_skills/education.md` | 3 别名 |
| 美容 | ⭐⭐⭐ 基础 | 5维度 | 单文件模式：`industry_skills/beauty.md` | 3 别名 |
| 中介（归为地产） | ⭐⭐⭐⭐⭐ 深度 | 6维度 | 目录模式（映射到 real_estate） | 同地产 |

### 3.2 地产行业6维度评分框架

```
房源获取 (25%) → 带看转化 (20%) → 社区渗透 (20%)
    → 线上获客 (15%) → 专业形象 (10%) → 数据运营 (10%)
```

对应模板：`prompts/diagnosis/real_estate.txt`（区别于通用行业的 `diagnosis/system.txt` 的5维度框架）

### 3.3 通用5行业5维度评分框架

```
定位 (20%) → 产品 (20%) → 渠道 (25%) → 内容 (15%) → 转化 (20%)
```

---

## 四、数据模型（前后端已对齐 v3.0）

只定义这条链路上真正需要流通的数据结构。

### 4.1 BusinessProfile（企业信息）

```python
@dataclass
class BusinessProfile:
    id: str                          # UUID（DB自动生成）
    business_name: str
    industry: str                    # 行业（12+地产别名 / 家装 / 餐饮 / 教培 / 美容）
    city: str
    product_desc: str                # 卖什么，自由文本
    price_range: str                 # 价格区间
    target_customers: str            # 目标客户，自由文本
    competitors: str                 # 竞品是谁，自由文本
    current_channels: str            # 现在怎么获客，自由文本
    monthly_revenue: str             # 月营业额（允许填"不便透露"）
    team_size: str                   # 几个人干
    biggest_pain: str                # 最大痛点，自由文本
    created_at: datetime
```

> 设计原则：表单字段尽量少，自由文本为主。小老板没耐心填结构化问卷，让他们用大白话说就行，AI负责理解和结构化。

### 4.2 DiagnosisReport（诊断报告）

```python
@dataclass
class DiagnosisReport:
    id: str
    business_id: str
    overall_score: int               # 0-100
    score_summary: str               # 一句话评分理由（新增）
    score_breakdown: dict            # 地产6维度 / 通用5维度
    top3_problems: list[Problem]     # Top3问题
    strategy_summary: str            # 一句话策略方向（这是给执行引擎的关键输入）
    this_week_focus: str             # 本周应该重点做的一件事（新增，替代 top3_recommendations）
    created_at: datetime

@dataclass
class Problem:
    severity: str                    # critical / major / minor（⚠️ 前后端对齐，禁止用 warning/info）
    category: str                    # 匹配维度名
    description: str
    quick_fix: str                   # 一个马上能做的事
```

### 4.3 SevenDayPlan（7天执行清单——核心数据）

```python
@dataclass
class SevenDayPlan:
    id: str
    diagnosis_id: str
    business_id: str                 # 新增：直接关联企业
    start_date: date
    theme: str                       # 本周主题
    goals: list[str]                 # 本周目标（2-3条）
    key_metrics: dict                # {新增客户:0, 咨询量:0, 成交量:0} 用于复盘对比
    days: list[DayPlan]              # 7天的计划

@dataclass
class DayPlan:
    day_label: str                   # "周一"、"周二"...（字段更名，原 day）
    focus: str                       # 当天的重点一句话
    tasks: list[Task]                # 2-5个任务

@dataclass
class Task:
    time_slot: str                   # "上午" / "下午" / "晚上"（字段更名，原 time 改为时间段）
    title: str                       # 任务标题
    how_to: str                      # 怎么做（1-2句话）
    checklist: list[str]             # 执行步骤（新增：小老板逐项打勾）
    done_criteria: str               # 什么叫完成了
    estimated_minutes: int           # 预估耗时（字段更名，原 minutes）
    done?: bool                      # 前端运行态打勾标记
```

> **硬约束**：每天不超过5个任务，总计不超过2小时；周六必安排"本周数据汇总"；周日休息或少量任务。

### 4.4 ReviewReport（复盘报告）

```python
@dataclass
class ReviewReport:
    id: str
    plan_id: str
    business_id: str                 # 新增：直接关联企业
    summary: str                     # 本周一句话总结
    numbers: dict                    # 从截图里提取的数字
    vs_target: list[MetricComparison]# 变更：数组（原dict），便于前端循环渲染
    what_worked: list[str]           # 做得好的
    what_didnt: list[str]            # 没做到的
    suggestions: list[str]           # 3条优化建议，可输入到下一周的执行引擎
    created_at: datetime

@dataclass
class MetricComparison:              # 新增（替代原dict嵌套）
    metric_name: str                 # 指标名：新增客户 / 咨询量 / 成交量...
    target: int
    actual: int
    achieved: bool
```

---

## 五、模块设计（v3.0 新增：技能 / Pipeline / 规则引擎 / 前端 4大模块）

### 5.1 模块1：AI诊断

**功能**：用户填完企业信息 → AI 输出一份诊断报告（支持 LLM 模式和本地规则模式双路）

**这条链路中诊断报告要解决的问题**：
1. 企业营销健康度打分（让老板意识到问题）
2. 指出Top3问题和方向性建议（告诉老板该往哪走）
3. 给出一句话策略方向（作为执行引擎的关键输入）
4. 本周重点1件事（作为老板的每日聚焦）

**Prompt 核心结构**：
```
角色：资深企业营销顾问
目标用户：不懂营销的小企业老板

输入：企业信息 + [可选：行业技能注入 skill_context]

输出（严格JSON）：
  overall_score / score_summary
  score_breakdown（地产6维度 / 通用5维度）
  top3_problems：每个 severity=major/minor/critical + quick_fix
  strategy_summary（一句话策略方向，具体可执行）
  this_week_focus（本周重点做的一件事）

约束：老板能看懂 / 不要黑话 / 不要空泛建议 / 策略方向必须具体
```

**关键规则**：
- 地产行业走专属模板 `diagnosis/real_estate.txt`（6维度）；其余行业走 `diagnosis/system.txt`（5维度）
- LLM 不可用或失败时 → 回退到 `rule_based_diagnosis.py` 的规则引擎（见 §5.6）

### 5.2 模块2：执行引擎（核心壁垒）

**功能**：拿着诊断报告的一句话策略方向 + 企业信息，生成一份7天任务清单。

**为什么这是壁垒**：市面上任何AI都能做"诊断"，但没有AI能做"把诊断变成每天具体做什么"。

**核心逻辑（5步翻译 + 约束验证 + 3次重试 + Fallback）**：

```
Step 1: 策略分解 — 识别动作线
Step 2: 为每条动作线生成子任务
Step 3: 按优先级和时间逻辑排布到7天（周一规划 / 周二-周五执行 / 周六汇总 / 周日休息）
Step 4: 每任务附执行细节：怎么做 + 检查项 checklist + 完成标准 + 预估耗时
Step 5: 设本周目标（为下周复盘准备基线）

→ 输出 JSON 后，进入「约束验证」：
   · 每天 ≤ 5 任务
   · 每天 ≤ 120 分钟
   · 每个任务都有 how_to + done_criteria
   → 不通过：LLM 重试（最多3次，带修正反馈）
   → 3次都不通过：回退本地 fallback_plan() 生成合规计划
```

**两个 Prompt 模板**：
- `executor/system.txt`：7天总体框架渲染（含 start_date 注入）
- `executor/day_template.txt`（v3.0 新增）：单日任务精细度 Prompt（how_to / checklist / done_criteria 要求）

**质量验证标准**：
- 老板拿到清单后，不需要任何额外思考就能开始执行
- 每个任务有明确的起止条件；7天的任务之间有递进关系（不是孤立堆砌）

### 5.3 模块3：数据复盘

**功能**：周末老板上传本周数据的截图/CSV，AI解析后生成复盘报告，并给下周建议。

**处理流程**：

```
用户上传（截图 / CSV 混合）
    │
    ├── 图片 → 多模态模型直接识别数字
    │         → 提取：新增客户、咨询量、成交量、每条内容数据
    │
    └── CSV  → pandas 读取结构化数据
              → 映射到标准指标名称（合并策略：先出现值优先）

    ▼
AI 对比分析（vs 本周 key_metrics 目标）：
  · 实际 vs 目标 → MetricComparison 对象（achieved 布尔值）
  · 哪些达标、哪些没达标
  · 识别异常（如播放量高但咨询量低）

    ▼
生成复盘报告：
  · 本周一句话总结
  · vs_target 对比数组
  · 做得好的 3 件 / 需改进 3 件
  · 下周 3 条建议（可直接喂给下周执行引擎）

  → 不通过 / LLM 失败 → fallback_report() 本地规则生成
```

**⚠️ 上传两阶段接口（v3.0 关键 API 设计，适配微信小程序 wx.uploadFile 单文件限制）**：

```
Stage 1（逐文件暂存）：
  POST /api/review/{plan_id}/upload
  body: FormData(file=file_object)   ← 每个文件单独调用一次
  返回：{ code:0, data:{ file_path, staged_count } }

Stage 2（汇总生成）：
  POST /api/review/{plan_id}/generate
  body: { files?: [file_path...] }   ← 不填则使用暂存区全部文件
  返回：{ code:0, data: ReviewReport }

兼容：POST /api/review/{plan_id}  （旧版一次性多文件上传，保留回退）
```

后端对应实现：[review.py](file:///Users/zhanggaozhang/TRAE-CN/Marketing%20Director/src/api/review.py)
前端对应实现：[request.ts](file:///Users/zhanggaozhang/TRAE-CN/Marketing%20Director/miniapp/api/request.ts#L62-L168)

### 5.4 模块4：技能导入模块（v3.0 新增，独立存储，按需注入）

**设计动机**：不同行业的诊断标准、获客方式、客户画像完全不同；通用 Prompt + 行业名称无法达到专业诊断深度。必须在诊断时刻**注入行业专属知识**。

**存储双模式**：

```
src/skills/
├── loader.py                    ← 行业别名映射 + 双模式加载 + 技能注入
├── real_estate/                 ← 目录模式（地产深度优先）
│   ├── diagnosis_criteria.md    ← 6维度评分标准
│   ├── metrics.md               ← 行业基准指标
│   ├── channels.md              ← 获客渠道策略
│   ├── customer_profiles.md     ← 客户画像
│   └── case_studies.md          ← 典型案例
│
└── industry_skills/             ← 单文件模式（其余5行业）
    ├── renovation.md            ← 家装
    ├── restaurant.md            ← 餐饮
    ├── education.md             ← 教培
    ├── beauty.md                ← 美容
    └── agency.md / generic.md   ← 中介(兼容) / 通用
```

**前后端别名对齐**（必须完全一致，12+ 地产别名）：
> 地产、房地产、房产、房产中介、房产经纪、中介、二手房中介、新房代理、商铺租赁、地产经纪、置业顾问、综合地产 → 全部映射到 real_estate 目录模式

**注入机制**：
- 后端：`DiagnosisAgent._select_template()` → 选模板；`load_prompt_with_skill()` → 注入变量 `skill_context`
- 前端：`miniapp/skills/index.ts` → 选择行业 icon、label、category 给用户展示

### 5.5 模块5：Pipeline 核心编排（v3.0 新增，串起完整链路）

**位置**：`src/services/pipeline.py`

**两个公开方法**：

```python
async def run_full_pipeline(business_id: str) -> dict:
    """
    唯一主流程：企业信息 → 诊断 → 7天计划
    全部三段均持久化到 SQLite；使用 UUID 防止 ID 冲突。
    返回：{ business: {...}, diagnosis: {...}, plan: {...} }
    """

async def run_weekly_review(plan_id: str, uploaded_files: list) -> dict:
    """周末复盘链路：上传文件 → 解析 → 复盘报告 → 持久化"""
```

**v3.0 关键修复（阻断性问题）**：
- 使用 UUID 生成业务 ID → 彻底消除 SQLAlchemy `UNIQUE constraint failed`
- 每个 Agent 失败 → 自动 fallback，不抛 ValueError
- 返回值中每段数据都携带自身 id + created_at + 关联 business_id / diagnosis_id / plan_id

### 5.6 模块6：本地规则诊断引擎（v3.0 新增，无 LLM 可用时的 Fallback）

**位置**：`src/services/rule_based_diagnosis.py`

**何时被调用**：
1. `.env` 中没有 `DEEPSEEK_API_KEY`
2. LLM 调用抛异常（网络/超时/限流）
3. LLM 返回的 JSON 解析失败（3次重试后）
4. 约束验证不通过（执行引擎 3 次重试都不达标）

**双框架支持**：
- 地产：REAL_ESTATE_DIMENSIONS 6 维度权重评分
- 通用：GENERIC_DIMENSIONS 5 维度权重评分

**打分规则参考**：
- 字段完整度（企业信息表单填了多少内容）
- 关键词匹配（行业 + 痛点 + 获客方式关键词命中）
- 行业基准对比（来自行业 skills 的 metrics.md）

---

## 六、前端小程序（v3.0 新增：营销军师 7页面 + 3TabBar + 状态管理）

### 6.1 TabBar（3个主入口，仅工作台/执行计划/复盘）

```
┌─────────────┬─────────────────┬─────────────────┐
│   工作台    │    执行计划     │      复盘       │  ← TabBar
│ (Tab 1)     │    (Tab 2)      │    (Tab 3)      │
└─────────────┴─────────────────┴─────────────────┘
```

对应 app.json 配置：[app.json](file:///Users/zhanggaozhang/TRAE-CN/Marketing%20Director/miniapp/app.json#L17-L41)

### 6.2 7个页面路由

| 页面路径 | 对应链路阶段 | 说明 |
|---|---|---|
| `pages/home/index` | 工作台首页 | 快捷入口：新建企业 / 继续上次 / 查看诊断 / 查看计划 / 去复盘 |
| `pages/profile/index` | 创建企业信息 | BusinessProfile 表单（11 字段，自由文本优先） |
| `pages/diagnosis/index` | 诊断报告展示 | overall_score 分数环 + top3_problems 卡片 + strategy_summary + this_week_focus |
| `pages/plan/index` | 7天执行清单总览 | theme + goals + 7 天 DayPlan 折叠卡片 |
| `pages/plan/day` | 单日任务详情 | 每个 Task 的 time_slot + how_to + checklist 打勾 + done_criteria |
| `pages/upload/index` | 上传本周截图/CSV | 支持多文件选择 → 两阶段上传（逐文件 stage → generate） |
| `pages/review/index` | 复盘报告展示 | summary + MetricComparison 列表 + what_worked/what_didnt 卡片 + suggestions |

### 6.3 3个全局复用组件（usingComponents 全局注册）

| 组件 | 路径 | 作用 |
|---|---|---|
| `<card>` | `components/card/` | 统一卡片样式（诊断项 / 任务项 / 复盘项） |
| `<loading>` | `components/loading/` | 请求中的加载状态统一 |
| `<empty-state>` | `components/empty-state/` | 空数据状态统一提示 |

### 6.4 全局状态管理（微信原生 globalData + 本地持久化）

[app.ts](file:///Users/zhanggaozhang/TRAE-CN/Marketing%20Director/miniapp/app.ts) 提供：

```typescript
interface GlobalState {
  businessId: string    // 链路 ID 1
  diagnosisId: string   // 链路 ID 2
  planId: string        // 链路 ID 3
  reviewId: string      // 链路 ID 4
  apiBase: string       // 默认 'http://localhost:8000/api'
}

saveState(key, value)   // 同时写 globalData + wx.setStorageSync
getState(key)           // 读 globalData
resetAll()              // 清空当前链路
restoreFromStorage()    // onLaunch 时自动恢复
```

### 6.5 全局类型定义（前端 types/index.ts，100% 对齐后端模型）

**Severity 枚举**：`'critical' | 'major' | 'minor'`
**6 大接口**：BusinessProfile / Problem / DiagnosisReport / Task / SevenDayPlan / MetricComparison / ReviewReport / GlobalState

### 6.6 API 请求统一封装（miniapp/api/request.ts）

- `get<T>(path)` / `post<T>(path, body)` → 统一 `{ code:0, data:T }` 解析
- `upload<T>(path, files)` → 两阶段上传：逐文件 stage → generate 汇总；失败回退 legacy 接口（兼容）

### 6.7 代码有效性检测方案（Phase 2 调优产物 v3.0 新增）

脚本位置：`miniapp/check.js`

6 类检测项：
1. 文件完整性（pages/ + components/ 引用文件存在）
2. 编译一致性（.ts 必须有对应的 .js）
3. 静态资源引用（图标、图片路径存在）
4. 引用关系正确性（usingComponents 路径可解析）
5. 冗余文件检测（未被引用的文件标记）
6. app.json pages 与实际文件对应

---

## 七、技术方案（v3.0 修正）

### 7.1 技术栈（MVP，已验证）

| 层级 | 技术 | 版本要求 | 理由 |
|------|------|---------|------|
| LLM 主力 | DeepSeek V3 | 需要 API Key | 中文能力强，成本极低 |
| LLM 多模态 | GPT-4o / 通义千问VL | 需要 API Key | 截图识别 |
| **本地规则引擎** | 自研 rule_based_diagnosis.py | 零依赖 | **无需 API Key 也能跑完整链路** |
| 后端 | Python + FastAPI | **Python 3.9+** | 异步、简洁 |
| 数据库 | SQLAlchemy + SQLite | 1.4+ | 零配置，MVP 够用 |
| 前端 | 微信小程序 + TypeScript | 基础库 3.0+ | 目标用户在小程序上 |
| Prompt 管理 | jinja2 + .txt | v3.0 含短名别名（diagnosis → system.txt） | 代码与 Prompt 分离 |
| 测试 | pytest + pytest-asyncio | 8.x | 149 测试已通过 |

**⚠️ Python 3.9 强约束（Hard Constraint）**：
```python
# ❌ 禁止：
files: list[str] | None = None
def f(req: GenerateRequest | None = None):

# ✅ 必须：
from typing import List, Optional
files: Optional[List[str]] = None
def f(req: Optional[GenerateRequest] = None):
```

### 7.2 项目目录（v3.0 合并后的真实目录）

```
ai-marketing-agent/                           ← 根：Marketing Director
├── miniapp/                                   ← 微信小程序前端
│   ├── pages/
│   │   ├── home/index.{ts,js,wxml,wxss,json}
│   │   ├── profile/...        ← 创建企业信息
│   │   ├── diagnosis/...      ← 诊断报告
│   │   ├── plan/index & day   ← 7天清单 + 单日详情
│   │   ├── upload/...         ← 上传截图/CSV
│   │   └── review/...         ← 复盘报告
│   ├── components/            ← 3全局组件（card/loading/empty-state）
│   ├── api/request.ts         ← get/post/upload（两阶段）
│   ├── types/index.ts         ← 全局类型
│   ├── skills/index.ts        ← 行业技能（12+ 地产别名）
│   ├── typings/global.d.ts    ← 微信全局类型补丁（Component/wx.*）
│   ├── static/tab-*.png       ← 6张 TabBar 图标
│   ├── app.json               ← TabBar + usingComponents + pages
│   ├── app.ts                 ← 全局状态（saveState/getState/resetAll）
│   ├── app.wxss               ← 全局样式
│   ├── check.js               ← 代码有效性检测脚本
│   ├── check-report.json      ← 最近一次检测结果
│   └── project.config.json    ← miniprogramRoot + typescript plugin
│
├── src/
│   ├── agents/                ← 3 个 Agent（诊断/执行/复盘）
│   │   ├── diagnosis.py       ← 行业模板选择器 + 技能注入 + fallback 规则
│   │   ├── executor.py        ← 5步翻译 + 约束验证(3次重试) + fallback_plan
│   │   └── reviewer.py        ← CSV/图片解析合并 + MetricComparison + fallback_report
│   │
│   ├── prompts/
│   │   ├── diagnosis/
│   │   │   ├── system.txt         ← 通用5行业5维度模板
│   │   │   └── real_estate.txt    ← 地产专属6维度模板
│   │   ├── executor/
│   │   │   ├── system.txt         ← 7天总体框架
│   │   │   └── day_template.txt   ← 单日任务精细度（checklist要求）
│   │   └── reviewer/
│   │       ├── report.txt         ← 复盘报告生成
│   │       └── parse_image.txt    ← 多模态截图数字提取
│   │
│   ├── models/                ← 4大业务模型（Python dataclass）
│   │   ├── business.py
│   │   ├── diagnosis.py
│   │   ├── execution.py       ← 含 DayPlan.validate_constraints()
│   │   └── review.py
│   │
│   ├── services/
│   │   ├── pipeline.py             ← 核心编排：UUID + 全链路
│   │   ├── llm.py                  ← LLM Provider（文本 + 多模态）
│   │   └── rule_based_diagnosis.py ← 无 LLM Fallback
│   │
│   ├── skills/                ← 技能导入模块
│   │   ├── loader.py          ← 别名映射 + dir/file 双模式
│   │   ├── real_estate/       ← 地产目录模式（6文件深度）
│   │   └── industry_skills/   ← 5行业单文件模式
│   │
│   ├── utils/document_parser.py   ← 图片/CSV解析 + 多数据源合并
│   │
│   ├── api/                    ← FastAPI 路由（15个端点）
│   │   ├── main.py             ← CORS + 路由注册 + /health
│   │   ├── business.py         ← POST /GET /business
│   │   ├── diagnosis.py        ← GET/POST /diagnosis/{bid}
│   │   ├── execution.py        ← POST /{did} + GET /{plan_id}
│   │   └── review.py           ← upload + generate + GET
│   │
│   ├── config/settings.py
│   └── db/models.py            ← 4张 ORM 表 + init_db() + SyncSession
│
├── tests/
│   ├── conftest.py             ← MockLLMProvider（⚠️ 路由顺序：执行教练→数据分析师→营销顾问）
│   ├── fixtures/industries.py  ← 5行业完整夹具（profile/diagnosis/executor/review/csv）
│   ├── test_diagnosis_agent.py
│   ├── test_executor_agent.py
│   ├── test_reviewer_agent.py
│   ├── test_models.py
│   ├── test_pipeline.py        ← 端到端测试（企业→诊断→计划；上传→复盘）
│   ├── test_prompt_templates.py
│   ├── test_prompt_snapshot.py ← 13条Prompt回归基线（⚠️ 更新需加 --update-snapshot）
│   ├── snapshots/*.snap        ← 6模板渲染哈希快照
│   └── 测试报告.md
│
├── data/
│   └── app.db                  ← SQLite 数据库（初始化时自动创建）
│
├── requirements.txt
├── project.config.json         ← miniprogramRoot: "miniapp/"
├── project-info.json           ← (⚠️ 非 app.json：根目录禁止放 app.json)
├── 开发思路文档.md              ← 本文档（v3.0 合并版）
└── 文档合并差距清单.md          ← 合并对照表
```

### 7.3 后端 API 15 个端点一览（统一前缀 `/api`）

| Method | Path | 说明 |
|---|---|---|
| GET | `/` | 版本信息 |
| GET | `/health` | 健康检查 |
| POST | `/business` | 创建企业档案（链路第 1 步） |
| GET | `/business/{id}` | 读取企业信息 |
| POST | `/diagnosis/{business_id}` | 触发诊断（链路第 2 步） |
| GET | `/diagnosis/{business_id}` | 读取最新诊断 |
| POST | `/execution/{diagnosis_id}` | 触发7天计划（链路第 3 步） |
| GET | `/execution/{plan_id}` | 读取7天计划 |
| POST | `/review/{plan_id}/upload` | **Stage 1**：逐文件暂存上传（wx.uploadFile 适配） |
| POST | `/review/{plan_id}/generate` | **Stage 2**：汇总暂存文件，生成复盘报告 |
| GET | `/review/{plan_id}` | 读取复盘报告 |
| POST | `/review/{plan_id}` | **Legacy**：旧版一次性多文件上传 |

---

## 八、MVP 开发计划（v3.0 实际完成度）

**唯一目标：跑通那条链路。**

### Phase 0：基础（1周） ✅ 完成

- [x] 项目目录创建（v3.0 实际 90+ 文件）
- [x] FastAPI 框架搭建 + CORS
- [x] LLMProvider 封装（DeepSeek 文本 + GPT-4o 多模态）
- [x] SQLite + SQLAlchemy ORM 初始化（4 张表）
- [x] 4 大数据模型定义（Python + TS 前后端对齐）
- [x] Prompt 模板加载（jinja2 + .txt）
- [x] **Python 3.9 兼容**（Optional / List 替换）

### Phase 1：链路（2周） ✅ 完成（v3.0 新增双模式+技能）

- [x] **模块1：诊断** — 企业信息 → 诊断报告
  - [x] 地产专属 6 维度模板 + 通用 5 维度模板
  - [x] 行业技能注入（loader.py 别名映射 + 双模式加载）
  - [x] **Rule-based Fallback 本地规则引擎**（无 API Key 也能跑）
- [x] **模块2：执行引擎** — 诊断 → 7天任务清单（**重点，最花时间**）
  - [x] 5 步翻译逻辑
  - [x] 硬约束验证（≤5任务 ≤120分钟）
  - [x] **3 次重试机制 + Fallback 本地生成**
  - [x] day_template.txt 单日精细度
- [x] **模块3：复盘** — 上传截图 → 复盘报告
  - [x] CSV 解析（Key-Value / 多列 / BOM / 空 CSV）
  - [x] 图片多模态解析（失败容错，不影响 CSV）
  - [x] 数据合并（多数据源，先出现值优先）
  - [x] **Fallback_report 本地规则生成**
- [x] pipeline.py 串联整条链路（**UUID 生成 + UNIQUE 冲突修复**）
- [x] 5 行业真实夹具 + 端到端跑通

### Phase 2：调优（1周） ✅ 完成

- [x] 诊断报告质量调优（5 行业参数化测试；地产 6 维度 vs 通用 5 维度）
- [x] 执行引擎任务质量调优（粒度、实操性、时间合理性）
- [x] 截图解析准确率调优 + 异常容错
- [x] **Prompt 回归测试 13 条基线 + 6 模板快照**（`tests/snapshots/*.snap`）
- [x] **149 测试全部通过**
- [x] 阻断性 3 问题修复：pipeline 重建 / severity 对齐 / fallback 机制
- [x] API 对接 15 端点验证 + wx.uploadFile 两阶段适配

### Phase 3：小程序（2周） ✅ 完成（超额）

- [x] 企业信息填写页 `pages/profile`
- [x] 诊断报告查看页 `pages/diagnosis`
- [x] 7 天任务卡片页（核心界面）`pages/plan` + `pages/plan/day`
- [x] 截图上传页 `pages/upload`
- [x] 复盘报告查看页 `pages/review`
- [x] **工作台首页** `pages/home`（TabBar 1）
- [x] **3 全局复用组件**（card / loading / empty-state）
- [x] **全局状态管理**（saveState / getState / resetAll + 本地缓存恢复）
- [x] **前端类型系统**（types/index.ts，前后端枚举对齐）
- [x] **API 封装 + 两阶段上传**（request.ts）
- [x] **代码有效性检测**（check.js，6 类检测项）

**继续不做的**：内容工坊、销售助手、用户系统、支付、数据分析仪表盘、多 Agent 架构。

---

## 九、14 条 Hard Constraints（v3.0 新增，必须写入工程规范）

任何新开发者接手项目时，先读这 14 条，否则必然踩坑。

### 小程序端（7 条）

1. **TS 必须编译**：`.ts` → 同目录 `.js`；`project.config.json` 需配置：
   ```json
   { "miniprogramRoot": "miniapp/", "useCompilerPlugins": ["typescript"] }
   ```
2. **根目录禁止放 app.json**：根目录叫 `project-info.json`，小程序真正的 `app.json` 在 `miniapp/app.json`。否则 DevTools 报「pages 不能为空」。
3. **pages 数组引用必须存在 .js 编译结果**：`miniapp/check.js` 检测脚本每次提交前运行一次。
4. **usingComponents 路径要可解析**：组件 `.json` 里引用的文件路径必须是物理存在的（检查脚本第 4 项）。
5. **Severity 枚举**：前端 `Problem.severity` 只能是 `'critical' | 'major' | 'minor'`，禁止用 `warning / info`。
6. **上传两阶段**：`api/upload<T>(path, files)` 优先用 `/upload + /generate`；失败自动回退 legacy `/review/{plan_id}` 一次性上传。
7. **apiBase 默认值**：小程序开发阶段默认 `http://localhost:8000/api`，发布时替换为生产域名。

### 后端 + 测试（7 条）

8. **Mock LLM 路由顺序（⚠️ 极易踩坑）**：
   ```
   执行教练 ("7天执行清单") → 数据分析师 ("复盘报告") → 营销顾问 ("营销诊断")
   ```
   禁止先匹配 "诊断" 字样，否则 executor/reviewer prompt 中「诊断结论」字样会被误判为诊断 Agent，返回 diagnosis JSON，导致执行引擎 days 为空。
9. **Mock 方法参数名**：`set_responses(diagnosis=..., executor=..., review=..., vision=...)`
   - 不是 `reviewer=`，不是 `image_parse=`。
10. **UUID 生成 ID**：测试夹具和 pipeline 必须 UUID，禁止硬编码 `id=123`（否则 UNIQUE constraint failed）。
11. **Agent 失败一律 fallback，不抛 ValueError**：
    - LLM 抛异常 → fallback
    - JSON 解析失败 → fallback
    - 执行引擎约束 3 次不通过 → fallback_plan（本地规则生成合规计划）
    - 复盘 Agent JSON 失败 → fallback_report
12. **Python 3.9 类型**：`Optional[X]` 不用 `X | None`；`List[X]` 不用 `list[X]`。
13. **行业维度**：
    - 地产 12+ 别名（中介 / 置业顾问 / 新房代理...）→ 6 维度（real_estate.txt）
    - 其他 5 行业 → 5 维度（system.txt）
14. **Prompt 模板短名别名**：
    ```python
    "diagnosis"    → "diagnosis/system.txt"
    "executor"     → "executor/system.txt"
    "reviewer"     → "reviewer/report.txt"
    ```

---

## 十、开发规范（v3.0 合并）

### 10.1 代码与 Prompt

- Python：PEP 8 + 类型注解（Python 3.9 兼容）
- TypeScript：严格模式；编译产出 .js 提交到仓库（小程序 DevTools 不支持动态编译）
- Prompt：`.txt` 文件存储 + jinja2 注入 + **Git 版本控制**；**禁止在代码里硬编码 Prompt**
- 每个 Prompt 文件头部标注：版本号 + 用途

### 10.2 测试

- 每个模块的 Prompt 用 3+ 真实案例跑测试
- 5 行业全量参数化执行（执行引擎 8×5=40 用例，复盘 4×5=20 用例）
- 验证输出 JSON Schema 格式正确
- 执行引擎额外验证约束（≤5 任务 ≤120 分钟）
- Prompt 修改后：`python3 -m pytest tests/test_prompt_snapshot.py --update-snapshot -v` 更新快照 → 提交
- 日常回归：`python3 -m pytest tests/ -v`（预期 149 passed）

### 10.3 关键原则（永远不变）

1. **永远从这条链路出发判断优先级**：任何功能/代码，如果不服务于"输入→诊断→7天计划→执行→复盘"，就是噪音，删掉
2. **任务清单是产品，不是中间产物**：执行引擎输出的 7 天清单质量直接决定产品价值
3. **用真实企业测试，不要用虚构案例**：虚构样本测不出真实问题
4. **先跑通再优化**：不要让诊断报告写得像麦肯锡，先让整条链路能跑完 → 本地规则 Fallback 保证这一点
5. **地产先行，其他行业延用同一框架**：不要为了新增行业新增一套代码，只加 skill 文件 + 行业别名映射

---

## 十一、附录

### 11.1 执行引擎翻译示例（与 v2.0 相同，保留）

**输入（诊断模块输出的一句话策略方向）**：
> 聚焦30-45岁改善型家庭，用抖音短视频展示装修案例获取咨询，配合老客户转介绍建立信任，本周重点启动短视频获客渠道。

**输出（执行引擎生成的7天清单）**：

```
本周主题：启动短视频获客，建立线上信任

本周目标：
  - 发布3条装修案例短视频
  - 获得10条有效咨询
  - 联系5个老客户做转介绍铺垫

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
周一：准备日
├── [上午-30分钟] 注册完善抖音企业号
│   怎么做：下载抖音→注册→选企业号→完善头像/简介/联系方式
│   Checklist：□ 下载抖音APP □ 切换企业号 □ 设置头像和简介 □ 绑定联系电话
│   完成标准：主页信息完整，头像和封面已设置
│
├── [上午-20分钟] 研究5个本地同行账号
│   怎么做：搜索"城市+装修"关键词，看哪些账号数据好，记录标题和选题
│   完成标准：记录5个爆款选题方向和标题
│
└── [下午-20分钟] 确定本周3条视频选题
    怎么做：结合同行爆款+自己的案例，挑3个选题（改造前后对比/避坑指南/价格透明化）
    完成标准：3个选题已定，各一句话描述

周二：第一条
├── [上午-30分钟] 写第1条视频脚本
│   怎么做：选题→写好开头钩子（3秒抓人）→展示案例→给出干货→引导私信
│   完成标准：脚本完成，能照着拍
│
└── [下午-30分钟] 拍摄+发布第1条视频
    怎么做：手机横屏拍→简单剪辑→加字幕→早上8点前发布
    完成标准：视频已发布

周三：第二条  ──> 周四：第三条+转介绍  ──> 周五：执行  ──> 周六：数据汇总 ──> 周日：休息或补漏
```

### 11.2 v2.0 → v3.0 变更摘要

| 项 | v2.0 | v3.0 |
|---|---|---|
| 版本时间 | 2026-07-29 | 2026-08-01 |
| 产品名 | 未命名 | **营销军师**（前端 TabBar 品牌） |
| 行业方案 | 仅目标用户描述 | 地产 6 维度 + 12 别名 / 5 行业 5 维度 + 单文件技能 |
| 运行模式 | 仅 LLM 模式 | **LLM 模式 + 本地规则引擎 Fallback 双模式** |
| 技能模块 | 无 | **独立技能模块（后端 loader + 前端 skills/index.ts）** |
| 前端 | 文档 §Phase3 计划 | **7 页面 + 3TabBar + 3 组件 + 状态管理 + 类型系统** |
| 上传接口 | 未详述 | **两阶段上传接口（stage/generate）** 适配 wx.uploadFile |
| 测试 | 未量化 | **149 passed / 13 Prompt 快照回归 / 5 行业参数化** |
| 数据模型 字段更名 | severity=warning/info | 统一 **critical/major/minor**；字段 day→day_label, time→time_slot, minutes→estimated_minutes, 新增 checklist；vs_target dict→数组 |
| Pipeline | 伪代码 | UUID 生成 + 全链路编排 + 三段持久化 |
| Hard Constraints | 无 | **14 条写入工程规范** |

---

本文档 v3.0 取代 v2.0 成为唯一开发思路参考文档。
